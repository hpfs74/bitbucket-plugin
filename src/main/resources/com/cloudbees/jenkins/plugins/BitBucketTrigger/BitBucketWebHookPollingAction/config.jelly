<?jelly escape-by-default='true'?>
<j:jelly xmlns:j="jelly:core" xmlns:st="jelly:stapler" xmlns:d="jelly:define" xmlns:l="/lib/layout" xmlns:t="/lib/hudson" xmlns:f="/lib/form">
  <f:section title="${%buildtriggerbadge.title}">
    <f:entry title="${%buildtriggerbadge.activated.title}" description="${%buildtriggerbadge.activated.description}">
      <f:textbox name="${it.FIELD_IGNORED_USERS}" value="${it.ignoredUsers}" />
    </f:entry>
  </f:section>
</j:jelly>